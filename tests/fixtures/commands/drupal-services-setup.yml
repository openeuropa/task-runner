- config: |
    my_settings:
      foo: bar
    drupal:
      service_parameters:
        settings:
          foo: ${my_settings.foo}
  expected:
    - contains: |
        parameters:
          settings:
            foo: bar
