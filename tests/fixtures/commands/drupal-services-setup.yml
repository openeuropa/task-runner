- config:
    drupal:
      service_parameters:
        cors.config:
          enabled: false
  expected:
    - contains: "parameters:"
    - contains: "cors.config:"
    - contains: "enabled: false"
